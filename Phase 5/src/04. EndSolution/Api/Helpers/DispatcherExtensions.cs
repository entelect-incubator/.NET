namespace Api.Helpers;

// Dispatcher extension methods are no longer required in the end solution because
// the custom dispatcher already supports concise Send/Query calls with type inference.
internal static class DispatcherExtensions
{
}
