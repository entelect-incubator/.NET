name: .NET - Phase 7 - Final Solution

on:
    push:
        branches: [ main, master ]
        paths:
            - "Phase 7/**"
            - ".github/workflows/dotnet-phase7-finalsolution.yml"
    pull_request:
        branches: [ main, master ]
        paths:
            - "Phase 7/**"
            - ".github/workflows/dotnet-phase7-finalsolution.yml"

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v4
            - name: Setup .NET
              uses: actions/setup-dotnet@v4
              with:
                  dotnet-version: 10.0.x
            - name: Install dependencies
              run: dotnet restore "./Phase 7/src/04. Step 4/Pezza.slnx"
            - name: Build
              run: dotnet build "./Phase 7/src/04. Step 4/Pezza.slnx" --configuration Release
                  --no-restore
