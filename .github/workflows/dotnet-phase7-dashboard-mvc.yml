name: .NET - Phase 7 - Dashboard - MVC

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
  dotnet-version: 10.0.x
    - name: Install dependencies
      run: dotnet restore "./Phase 7/Dashboard/MVC/src/03. Step 3/Pezza.slnx"
    - name: Build
      run: dotnet build "./Phase 7/Dashboard/MVC/src/03. Step 3/Pezza.slnx" --configuration Release --no-restore
